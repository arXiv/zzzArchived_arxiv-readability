labels:
  subsystem: labs
database_url: psql://postgres@db:5432/postgres
redis:
  host: readability-redis-master
  port: 6379
namespace: development
imageTag: latest
secret_key: thisisnotasecret!
script_name: /
deploymentName: readability
serviceName: readability
ingressName: readability

ingress:
  host: changeme
  path: /


database:
  secret:
    name: secret-name
    key: key-in-secret

redis:
  secret:
    name: secret-name
    key: key-in-secret

django:
  secret:
    name: secret-name
    key: key-in-secret
